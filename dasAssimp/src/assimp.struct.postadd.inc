// this file is generated via daScript automatic C++ binder
// all user modifications will be lost after this file is re-generated

initRecAnnotation(ann_aiVector3D,lib);
initRecAnnotation(ann_aiVector2D,lib);
initRecAnnotation(ann_aiColor4D,lib);
initRecAnnotation(ann_aiMatrix3x3,lib);
initRecAnnotation(ann_aiMatrix4x4,lib);
initRecAnnotation(ann_aiQuaternion,lib);
initRecAnnotation(ann_aiPlane,lib);
initRecAnnotation(ann_aiRay,lib);
initRecAnnotation(ann_aiColor3D,lib);
initRecAnnotation(ann_aiString,lib);
initRecAnnotation(ann_aiMemoryInfo,lib);
initRecAnnotation(ann_aiImporterDesc,lib);
initRecAnnotation(ann_aiLogStream,lib);
initRecAnnotation(ann_aiPropertyStore,lib);
initRecAnnotation(ann_aiTexel,lib);
initRecAnnotation(ann_aiTexture,lib);
initRecAnnotation(ann_aiFace,lib);
initRecAnnotation(ann_aiVertexWeight,lib);
initRecAnnotation(ann_aiBone,lib);
initRecAnnotation(ann_aiAnimMesh,lib);
initRecAnnotation(ann_aiMesh,lib);
initRecAnnotation(ann_aiLight,lib);
initRecAnnotation(ann_aiCamera,lib);
initRecAnnotation(ann_aiUVTransform,lib);
initRecAnnotation(ann_aiMaterialProperty,lib);
initRecAnnotation(ann_aiMaterial,lib);
initRecAnnotation(ann_aiVectorKey,lib);
initRecAnnotation(ann_aiQuatKey,lib);
initRecAnnotation(ann_aiMeshKey,lib);
initRecAnnotation(ann_aiMeshMorphKey,lib);
initRecAnnotation(ann_aiNodeAnim,lib);
initRecAnnotation(ann_aiMeshAnim,lib);
initRecAnnotation(ann_aiMeshMorphAnim,lib);
initRecAnnotation(ann_aiAnimation,lib);
initRecAnnotation(ann_aiMetadataEntry,lib);
initRecAnnotation(ann_aiMetadata,lib);
initRecAnnotation(ann_aiNode,lib);
initRecAnnotation(ann_aiScene,lib);
